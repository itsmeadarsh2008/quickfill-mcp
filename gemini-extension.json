{
  "name": "quickfill-mcp",
  "version": "1.2.5",
  "description": "Generative UI Toolkit for AI Agents via MCP",
  "mcpServers": {
    "quickfill": {
      "command": "node",
      "args": ["${extensionPath}/dist/index.js"],
      "cwd": "${extensionPath}"
    }
  }
}
